{% extends '@Hotel/layout.html.twig' %}

{% block content %}
    <div class="col-lg-12">
        {{ form_start(form) }}
        <div class="col-lg-12">
            <div class="form-group">{{ form_row(form.arrival) }}</div>
            <div class="form-group">{{ form_row(form.departure) }}</div>
            <div class="form-group">{{ form_row(form.check) }}</div>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock content %}