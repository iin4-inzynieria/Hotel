<nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">Hotele WSB!</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                {% if current is not defined %}
                    {% set current = '' %}
                {% endif %}
                <li class="{% if current == 'offer' %}active{% endif %}"><a href="{{ path('hotel_rooms') }}">Oferta <span class="sr-only">(current)</span></a></li>
                <li class="{% if current == 'reservation' %}active{% endif %}"><a href="{{ path('hotel_reservation') }}">Rezerwacja</a></li>
                <li class="{% if current == 'restaurant' %}active{% endif %}"><a href="#">Restauracja</a></li>
                <li class="{% if current == 'contact' %}active{% endif %}"><a href="{{ path('hotel_contact') }}">Kontakt</a></li>

            </ul>

        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>